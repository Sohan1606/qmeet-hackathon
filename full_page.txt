"use client"

import { useState, useEffect, useCallback } from "react"
import { ArrowRight, Play, Upload, FileText, Check, Zap, Users, BarChart3, Clock, Shield, Mail, Target, ChevronRight, Star, Sparkles, TrendingUp, DollarSign, Award, X, User, LogOut, Settings as SettingsIcon, Calendar, Menu } from "lucide-react"
import { useRouter } from "next/navigation"
import axios from "axios"

const API_URL = process.env.NEXT_PUBLIC_API_URL || "http://127.0.0.1:8000"

const DEMO_TRANSCRIPT = "John (Project Manager): Good morning everyone. Lets discuss the Q3 product launch. Sarah (Designer): I finished the initial mockups last week but need feedback from the dev team. Mike (Developer): Sorry Sarah, I havent reviewed them. I will review all mockups and send feedback by Friday. John: Great. Mike, can you also prepare the technical architecture document for the new payment gateway? Mike: Yes, I will have the technical architecture document ready by next Wednesday. High priority. Sarah: I will finalize UI designs for the payment flow once Mike shares the architecture. Complete within 3 days after receiving his document. John: Perfect. About the marketing campaign - we havent decided on the launch date. Priya (Marketing): I think we should target October 15th. But I need the product demo video script first. John: Priya, can you draft the initial marketing campaign strategy document by next Monday? Priya: Absolutely. I will have the complete marketing strategy ready by Monday. I will need your approval on the budget. John: I will review and approve the budget within 24 hours. Also, everyone fill out the project status update form by tomorrow EOD. Mike: One concern - our current server infrastructure might not handle the launch load. We should do load testing. John: Good point. Mike, coordinate with DevOps and schedule load testing for the week of September 20th. Mike: Will do. I will set that up and send calendar invites. Sarah: Should we schedule a design review meeting next week? John: Yes, next Thursday at 2 PM. Sarah, send out the meeting invite. Sarah: Sure, I will send the invite by end of today. Priya: We havent decided on the influencer partnership strategy. John: Lets defer that to next weeks meeting."

// COOKIE BANNER
function CookieBanner() {
  const [show, setShow] = useState(false)
  useEffect(() => {
    const accepted = localStorage.getItem("qmeet_cookies")
    if (!accepted) setTimeout(() => setShow(true), 2000)
  }, [])
  const accept = () => { localStorage.setItem("qmeet_cookies", "accepted"); setShow(false) }
  const reject = () => { localStorage.setItem("qmeet_cookies", "rejected"); setShow(false) }
  if (!show) return null
  return (
    <div className="fixed bottom-4 left-4 right-4 md:right-auto md:max-w-md z-50 bg-white rounded-xl shadow-2xl border border-gray-200 p-4 animate-fade-in-up">
      <div className="flex items-start gap-3">
        <div className="w-8 h-8 rounded-lg bg-[#2383E2]/10 flex items-center justify-center flex-shrink-0">
          <Shield className="w-4 h-4 text-[#2383E2]" />
        </div>
        <div className="flex-1">
          <h3 className="text-sm font-bold text-gray-900 mb-1">We value your privacy</h3>
          <p className="text-xs text-gray-600 mb-3 leading-relaxed">We use cookies to enhance browsing and analyze traffic. See our <a href="/privacy" className="text-[#2383E2] underline font-semibold">Privacy Policy</a>.</p>
          <div className="flex items-center gap-2">
            <button onClick={accept} className="px-3 py-1.5 bg-[#2383E2] text-white rounded-lg text-xs font-semibold hover:bg-[#1a6dc4]">Accept all</button>
            <button onClick={reject} className="px-3 py-1.5 border border-gray-200 text-gray-700 rounded-lg text-xs font-semibold hover:bg-gray-50">Essential only</button>
          </div>
        </div>
        <button onClick={reject} className="text-gray-400 hover:text-gray-600 flex-shrink-0"><X className="w-4 h-4" /></button>
      </div>
    </div>
  )
}

// PROFILE DROPDOWN
function ProfileDropdown({ user }) {
  const [open, setOpen] = useState(false)
  const router = useRouter()
  const initials = user?.name ? user.name.split(" ").map(n => n[0]).join("").substring(0, 2).toUpperCase() : "U"
  const menuItems = [
    { icon: BarChart3, label: "Dashboard", href: "/app" },
    { icon: Calendar, label: "Meetings", href: "/app/meetings" },
    { icon: Users, label: "Team", href: "/app/team" },
    { icon: SettingsIcon, label: "Settings", href: "/app/settings" }
  ]
  const handleSignOut = () => {
    localStorage.removeItem("qmeet_user")
    sessionStorage.removeItem("qmeet_intro_shown")
    window.location.href = "/"
  }
  return (
    <div className="relative">
      <button onClick={() => setOpen(!open)} className="flex items-center gap-2 p-1 rounded-lg hover:bg-gray-100 transition-colors">
        <div className="w-8 h-8 rounded-full bg-gradient-to-br from-[#2383E2] to-blue-700 flex items-center justify-center text-white text-xs font-bold">{initials}</div>
      </button>
      {open && (
        <>
          <div className="fixed inset-0 z-40" onClick={() => setOpen(false)}></div>
          <div className="absolute right-0 mt-2 w-56 bg-white rounded-lg shadow-lg border border-gray-200 py-1 z-50">
            <div className="px-3 py-2 border-b border-gray-100">
              <p className="text-sm font-semibold text-gray-900">{user?.name || "User"}</p>
              <p className="text-xs text-gray-500 truncate">{user?.email || ""}</p>
            </div>
            {menuItems.map((item, i) => (
              <button key={i} onClick={() => { router.push(item.href); setOpen(false) }} className="w-full flex items-center gap-2 px-3 py-2 text-sm text-gray-700 hover:bg-gray-50 transition-colors">
                <item.icon className="w-3.5 h-3.5" />{item.label}
              </button>
            ))}
            <div className="border-t border-gray-100 mt-1 pt-1">
              <button onClick={handleSignOut} className="w-full flex items-center gap-2 px-3 py-2 text-sm text-red-600 hover:bg-red-50 transition-colors">
                <LogOut className="w-3.5 h-3.5" />Sign out
              </button>
            </div>
          </div>
        </>
      )}
    </div>
  )
}

// CINEMATIC INTRO - Shows only on FIRST visit ever
function CinematicIntro({ onComplete }) {
  const [step, setStep] = useState(0)
  useEffect(() => {
    const timers = [
      setTimeout(() => setStep(1), 300),
      setTimeout(() => setStep(2), 1200),
      setTimeout(() => setStep(3), 2400),
      setTimeout(() => setStep(4), 3200),
      setTimeout(() => setStep(5), 3800),
      setTimeout(() => onComplete(), 4400)
    ]
    return () => timers.forEach(clearTimeout)
  }, [onComplete])
  return (
    <div className="fixed inset-0 bg-white z-[300] flex items-center justify-center overflow-hidden" style={{ opacity: step >= 5 ? 0 : 1, transition: "opacity 0.6s cubic-bezier(0.4, 0, 0.2, 1)" }}>
      <div className="absolute inset-0 subtle-grid opacity-40"></div>
      <div className="absolute" style={{ opacity: step >= 1 ? 0.15 : 0, transition: "opacity 1s" }}>
        <div className="w-[500px] h-[500px] rounded-full border-2 border-[#2383E2] animate-rotate-slow"></div>
      </div>
      <div className="absolute" style={{ opacity: step >= 1 ? 0.1 : 0, transition: "opacity 1s" }}>
        <div className="w-[700px] h-[700px] rounded-full border-2 border-[#2383E2] animate-rotate-slow" style={{ animationDirection: "reverse", animationDuration: "30s" }}></div>
      </div>
      <div className="relative text-center">
        <div className="mb-8 flex justify-center" style={{ opacity: step >= 1 ? 1 : 0, transform: step >= 1 ? "scale(1)" : "scale(0.5)", transition: "all 0.8s cubic-bezier(0.16, 1, 0.3, 1)" }}>
          <div className="relative">
            <div className="w-16 h-16 rounded-2xl bg-[#2383E2] flex items-center justify-center shadow-2xl shadow-[#2383E2]/30">
              <Zap className="w-8 h-8 text-white" />
            </div>
            <div className="absolute inset-0 rounded-2xl bg-[#2383E2] animate-ping opacity-20"></div>
          </div>
        </div>
        <div className="mb-6 overflow-hidden">
          <div className="flex justify-center">
            {"QMEET".split("").map((char, i) => (
              <span key={i} className="inline-block text-[80px] md:text-[120px] font-black tracking-tighter text-gray-900" style={{ opacity: step >= 2 ? 1 : 0, transform: step >= 2 ? "translateY(0)" : "translateY(80px)", transition: `all 0.9s cubic-bezier(0.16, 1, 0.3, 1) ${i * 100}ms` }}>
                {char}
              </span>
            ))}
          </div>
        </div>
        <div className="text-sm md:text-base text-gray-500 tracking-[0.3em] uppercase font-semibold mb-8" style={{ opacity: step >= 3 ? 1 : 0, transform: step >= 3 ? "translateY(0)" : "translateY(10px)", transition: "all 0.8s cubic-bezier(0.16, 1, 0.3, 1)" }}>
          Autonomous Meeting Intelligence
        </div>
        <div className="w-48 h-[2px] bg-gray-200 mx-auto overflow-hidden rounded-full" style={{ opacity: step >= 4 ? 1 : 0, transition: "opacity 0.4s" }}>
          <div className="h-full bg-[#2383E2] rounded-full" style={{ width: step >= 4 ? "100%" : "0%", transition: "width 1.2s cubic-bezier(0.4, 0, 0.2, 1)" }}></div>
        </div>
        <div className="flex items-center justify-center gap-2 mt-6" style={{ opacity: step >= 4 ? 1 : 0, transition: "opacity 0.6s" }}>
          {[0, 1, 2, 3, 4, 5].map(i => (
            <div key={i} className="w-1.5 h-1.5 rounded-full bg-[#2383E2]" style={{ animation: step >= 4 ? `pulseDot 1.5s ease-in-out ${i * 200}ms infinite` : "none" }}></div>
          ))}
        </div>
        <div className="text-[11px] text-gray-400 mt-4 font-mono tracking-widest uppercase" style={{ opacity: step >= 4 ? 1 : 0, transition: "opacity 0.6s" }}>
          6 Agents Ãƒâ€šÃ‚Â· Initializing
        </div>
      </div>
    </div>
  )
}

export default function HomePage() {
  const [showIntro, setShowIntro] = useState(false)
  const [checkedIntro, setCheckedIntro] = useState(false)
  const [mode, setMode] = useState("demo")
  const [transcript, setTranscript] = useState("")
  const [isDragging, setIsDragging] = useState(false)
  const [error, setError] = useState("")
  const [scrolled, setScrolled] = useState(false)
  const [user, setUser] = useState(null)
  const router = useRouter()

  // Check for logged in user AND first visit
  useEffect(() => {
    const storedUser = localStorage.getItem("qmeet_user")
    if (storedUser) {
      try { setUser(JSON.parse(storedUser)) } catch (e) {}
    }

    // Show intro ONLY on the very first visit ever
    const introEverShown = sessionStorage.getItem("qmeet_intro_shown")
    if (!introEverShown) {
      setShowIntro(true)
    }
    setCheckedIntro(true)
  }, [])

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 20)
    window.addEventListener("scroll", handleScroll)
    return () => window.removeEventListener("scroll", handleScroll)
  }, [])

  const handleIntroComplete = () => {
    sessionStorage.setItem("qmeet_intro_shown", "true")
    setShowIntro(false)
  }

  const processTranscript = async (text, title) => {
    setError("")
    localStorage.removeItem("qmeet_result")
    router.push("/processing")
    try {
      const formData = new FormData()
      formData.append("transcript_text", text)
      formData.append("meeting_title", title || "Team Meeting")
      formData.append("user_id", "demo-user")
      formData.append("participants_count", "4")
      const response = await axios.post(API_URL + "/api/process-meeting", formData, { headers: { "Content-Type": "multipart/form-data" }, timeout: 120000 })
      localStorage.setItem("qmeet_result", JSON.stringify(response.data))
      localStorage.setItem("nexus_result", JSON.stringify(response.data))
    } catch (err) { setError(err.message) }
  }

  const processAudio = async (file) => {
    localStorage.removeItem("qmeet_result")
    router.push("/processing")
    try {
      const formData = new FormData()
      formData.append("file", file)
      formData.append("meeting_title", file.name.replace(/\.[^/.]+$/, ""))
      formData.append("user_id", "demo-user")
      formData.append("participants_count", "4")
      const response = await axios.post(API_URL + "/api/process-meeting", formData, { headers: { "Content-Type": "multipart/form-data" }, timeout: 300000 })
      localStorage.setItem("qmeet_result", JSON.stringify(response.data))
      localStorage.setItem("nexus_result", JSON.stringify(response.data))
    } catch (err) { setError(err.message) }
  }

  const handleDrop = useCallback((e) => {
    e.preventDefault()
    setIsDragging(false)
    const file = e.dataTransfer.files[0]
    if (file) processAudio(file)
  }, [])

  // Don't render anything until we've checked intro state (prevents flash)
  if (!checkedIntro) {
    return <div className="min-h-screen bg-white"></div>
  }

  // Show intro on first ever visit
  if (showIntro) {
    return <CinematicIntro onComplete={handleIntroComplete} />
  }

  return (
    <div className="min-h-screen bg-white animate-fade-in">

      {/* Navigation with proper login/signup flow */}
      <header className={"border-b border-gray-100 bg-white/80 backdrop-blur-xl sticky top-0 z-40 transition-all " + (scrolled ? "shadow-sm" : "")}>
        <div className="max-w-6xl mx-auto px-6 h-14 flex items-center justify-between">
          <div className="flex items-center gap-8">
            <a href="/" className="flex items-center gap-2">
              <div className="w-7 h-7 rounded-lg bg-[#2383E2] flex items-center justify-center shadow-sm">
                <Zap className="w-4 h-4 text-white" />
              </div>
              <span className="text-[15px] font-bold text-gray-900">QMEET</span>
              <span className="text-[9px] text-[#2383E2] font-semibold tracking-widest hidden md:inline">ENTERPRISE</span>
            </a>
            <nav className="hidden md:flex items-center gap-6">
              <a href="#features" className="text-[13px] text-gray-600 hover:text-gray-900 font-medium transition-colors">Product</a>
              <a href="/app/integrations" className="text-[13px] text-gray-600 hover:text-gray-900 font-medium transition-colors">Integrations</a>
              <a href="#pricing" className="text-[13px] text-gray-600 hover:text-gray-900 font-medium transition-colors">Pricing</a>
              <a href="/contact" className="text-[13px] text-gray-600 hover:text-gray-900 font-medium transition-colors">Contact</a>
            </nav>
          </div>
          <div className="flex items-center gap-3">
            {user ? (
              <>
                <button onClick={() => router.push("/app")} className="text-[13px] text-gray-700 hover:text-gray-900 font-medium">Go to Dashboard</button>
                <ProfileDropdown user={user} />
              </>
            ) : (
              <>
                <button onClick={() => router.push("/login")} className="text-[13px] text-gray-700 hover:text-gray-900 font-medium">Log in</button>
                <button onClick={() => router.push("/signup")} className="text-[13px] px-4 py-1.5 bg-[#2383E2] text-white rounded-md font-semibold hover:bg-[#1a6dc4] transition-all shadow-sm hover:shadow-md">
                  Sign up free
                </button>
              </>
            )}
          </div>
        </div>
      </header>

      {/* Hero */}
      <section className="relative overflow-hidden">
        <div className="absolute inset-0 subtle-grid opacity-30 pointer-events-none"></div>
        <div className="absolute top-40 left-1/2 -translate-x-1/2 w-[600px] h-[600px] bg-blue-100/40 rounded-full blur-[100px] pointer-events-none"></div>
        
        <div className="max-w-6xl mx-auto px-6 py-20 md:py-28 relative">
          <div className="max-w-3xl mx-auto text-center">
            <div className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-white border border-gray-200 shadow-sm mb-6 animate-fade-in-up">
              <div className="flex items-center gap-1">
                {[1,2,3,4,5].map(i => <Star key={i} className="w-3 h-3 fill-yellow-400 text-yellow-400" />)}
              </div>
              <span className="text-[11px] text-gray-700 font-medium">Rated #1 AI Meeting Tool of 2026</span>
              <ChevronRight className="w-3 h-3 text-gray-400" />
            </div>

            <h1 className="text-4xl md:text-6xl lg:text-7xl font-bold text-gray-900 leading-[1.05] tracking-tight mb-6 animate-fade-in-up" style={{ animationDelay: "0.1s" }}>
              Meetings end.<br />
              <span className="text-shimmer">Nothing gets lost.</span>
            </h1>

            <p className="text-lg md:text-xl text-gray-600 leading-relaxed mb-8 max-w-2xl mx-auto animate-fade-in-up" style={{ animationDelay: "0.2s" }}>
              QMEET deploys 6 autonomous AI agents that extract every commitment, track every deadline, and send follow-ups automatically <strong className="text-gray-900">until every action item is resolved</strong>.
            </p>

            <div className="flex flex-col sm:flex-row items-center justify-center gap-3 mb-10 animate-fade-in-up" style={{ animationDelay: "0.3s" }}>
              {user ? (
                <button onClick={() => router.push("/app")} className="group flex items-center gap-2 px-6 py-3 bg-[#2383E2] text-white rounded-lg font-semibold text-sm hover:bg-[#1a6dc4] transition-all shadow-lg shadow-[#2383E2]/25 hover:shadow-xl hover:-translate-y-0.5">
                  Go to Dashboard
                  <ArrowRight className="w-4 h-4 transition-transform group-hover:translate-x-0.5" />
                </button>
              ) : (
                <button onClick={() => router.push("/signup")} className="group flex items-center gap-2 px-6 py-3 bg-[#2383E2] text-white rounded-lg font-semibold text-sm hover:bg-[#1a6dc4] transition-all shadow-lg shadow-[#2383E2]/25 hover:shadow-xl hover:-translate-y-0.5">
                  Get Started Free
                  <ArrowRight className="w-4 h-4 transition-transform group-hover:translate-x-0.5" />
                </button>
              )}
              <button onClick={() => document.getElementById("demo").scrollIntoView({ behavior: "smooth" })} className="flex items-center gap-2 px-6 py-3 text-gray-700 hover:text-gray-900 font-medium text-sm border border-gray-200 rounded-lg hover:border-gray-400 hover:bg-gray-50 transition-all">
                <Play className="w-4 h-4" />
                See live demo
              </button>
            </div>

            <div className="flex items-center justify-center gap-6 text-xs text-gray-500 animate-fade-in-up" style={{ animationDelay: "0.4s" }}>
              <div className="flex items-center gap-1.5"><Check className="w-3.5 h-3.5 text-green-600" /><span>Free forever plan</span></div>
              <div className="flex items-center gap-1.5"><Check className="w-3.5 h-3.5 text-green-600" /><span>No credit card required</span></div>
              <div className="flex items-center gap-1.5"><Check className="w-3.5 h-3.5 text-green-600" /><span>SOC 2 compliant</span></div>
            </div>
          </div>

          {/* Product Preview */}
          <div className="mt-16 max-w-5xl mx-auto animate-fade-in-up" style={{ animationDelay: "0.5s" }}>
            <div className="rounded-xl border border-gray-200 shadow-2xl overflow-hidden bg-white hover-lift">
              <div className="border-b border-gray-100 bg-gray-50 px-4 py-2.5 flex items-center gap-2">
                <div className="flex gap-1.5">
                  <div className="w-2.5 h-2.5 rounded-full bg-red-400"></div>
                  <div className="w-2.5 h-2.5 rounded-full bg-yellow-400"></div>
                  <div className="w-2.5 h-2.5 rounded-full bg-green-400"></div>
                </div>
                <div className="flex-1 text-center"><span className="text-xs text-gray-500 font-mono">qmeet.ai/dashboard</span></div>
              </div>
              <div className="grid grid-cols-4 min-h-[440px]">
                <div className="bg-[#FBFBFA] border-r border-gray-100 p-3">
                  <div className="text-[10px] font-semibold text-gray-400 uppercase tracking-wider mb-2 px-2">Workspace</div>
                  <div className="space-y-0.5">
                    <div className="px-2 py-1.5 rounded bg-white text-gray-900 text-[13px] font-medium flex items-center gap-2 shadow-sm"><BarChart3 className="w-3.5 h-3.5" /> Dashboard</div>
                    <div className="px-2 py-1.5 rounded text-gray-600 text-[13px] font-medium flex items-center gap-2"><Users className="w-3.5 h-3.5" /> Meetings<span className="ml-auto text-[10px] bg-gray-100 px-1 py-0 rounded text-gray-500">12</span></div>
                    <div className="px-2 py-1.5 rounded text-gray-600 text-[13px] font-medium flex items-center gap-2"><Target className="w-3.5 h-3.5" /> Tasks<span className="ml-auto text-[10px] bg-red-100 px-1 py-0 rounded text-red-600">3</span></div>
                    <div className="px-2 py-1.5 rounded text-gray-600 text-[13px] font-medium flex items-center gap-2"><TrendingUp className="w-3.5 h-3.5" /> Analytics</div>
                  </div>
                </div>
                <div className="col-span-3 p-6 bg-white">
                  <div className="flex items-baseline justify-between mb-4">
                    <div>
                      <div className="text-xs text-gray-500 mb-1 font-semibold uppercase tracking-wide">Meeting Effectiveness</div>
                      <div className="flex items-baseline gap-2">
                        <span className="text-4xl font-bold text-gray-900">82</span>
                        <span className="text-gray-400 text-sm">/ 100</span>
                        <span className="text-xs text-green-700 font-semibold ml-2 bg-green-50 px-2 py-0.5 rounded-full">+12%</span>
                      </div>
                    </div>
                    <div className="text-right">
                      <div className="text-xs text-gray-500 mb-1 font-semibold uppercase tracking-wide">Saved</div>
                      <div className="text-2xl font-bold text-[#2383E2]">Rs 8,42,000</div>
                    </div>
                  </div>
                  <div className="grid grid-cols-3 gap-3 mb-4">
                    <div className="p-3 bg-gray-50 rounded-lg border border-gray-100"><div className="text-2xl font-bold text-gray-900">8</div><div className="text-xs text-gray-500">Action items</div></div>
                    <div className="p-3 bg-gray-50 rounded-lg border border-gray-100"><div className="text-2xl font-bold text-gray-900">5</div><div className="text-xs text-gray-500">Completed</div></div>
                    <div className="p-3 bg-gray-50 rounded-lg border border-gray-100"><div className="text-2xl font-bold text-gray-900">1</div><div className="text-xs text-gray-500">At risk</div></div>
                  </div>
                  <div className="space-y-2">
                    {[
                      { name: "Mike Chen", task: "Technical architecture doc", status: "In progress", color: "blue" },
                      { name: "Sarah Kim", task: "UI designs for payment", status: "Pending", color: "gray" },
                      { name: "Priya Sharma", task: "Marketing strategy", status: "Completed", color: "green" }
                    ].map((item, i) => (
                      <div key={i} className="flex items-center gap-3 p-2.5 border border-gray-100 rounded-lg hover:bg-gray-50 transition-colors">
                        <div className="w-7 h-7 rounded-full bg-[#2383E2]/10 flex items-center justify-center text-xs font-semibold text-[#2383E2]">{item.name[0]}</div>
                        <div className="flex-1 min-w-0">
                          <div className="text-xs font-medium text-gray-900">{item.task}</div>
                          <div className="text-xs text-gray-500">{item.name}</div>
                        </div>
                        <span className={"text-xs px-2 py-0.5 rounded-full font-medium " + (item.color === "green" ? "bg-green-100 text-green-700" : item.color === "blue" ? "bg-blue-100 text-blue-700" : "bg-gray-100 text-gray-600")}>{item.status}</span>
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Trust Bar */}
      <section className="border-y border-gray-100 py-10 bg-[#FBFBFA]">
        <div className="max-w-6xl mx-auto px-6">
          <p className="text-center text-[11px] font-semibold text-gray-500 uppercase tracking-widest mb-6">Trusted by teams at</p>
          <div className="flex items-center justify-center gap-x-12 gap-y-4 flex-wrap">
            {["Microsoft", "Google", "Amazon", "Stripe", "Notion", "Linear", "Figma", "Airbnb"].map(c => (
              <span key={c} className="text-lg font-bold text-gray-400 hover:text-gray-600 transition-colors">{c}</span>
            ))}
          </div>
        </div>
      </section>

      {/* Live Impact */}
      <section className="py-16 px-6 bg-white border-b border-gray-100">
        <div className="max-w-6xl mx-auto">
          <div className="text-center mb-10">
            <span className="text-[11px] font-semibold text-[#2383E2] uppercase tracking-widest">Real Impact</span>
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mt-3">QMEET is already changing how teams work</h2>
          </div>
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
            {[
              { value: "42,847", label: "Tasks resolved", icon: Target },
              { value: "99.2%", label: "Accuracy rate", icon: Award },
              { value: "3.4M hrs", label: "Time reclaimed", icon: Clock },
              { value: "Rs 12Cr", label: "Cost saved", icon: DollarSign }
            ].map((stat, i) => (
              <div key={i} className="p-6 bg-white rounded-xl border border-gray-200 hover-lift">
                <div className="w-9 h-9 rounded-lg bg-[#2383E2]/10 flex items-center justify-center mb-3">
                  <stat.icon className="w-4 h-4 text-[#2383E2]" />
                </div>
                <div className="text-3xl font-bold text-gray-900 mb-1">{stat.value}</div>
                <div className="text-sm text-gray-500">{stat.label}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Problem Stats */}
      <section className="py-20 px-6 bg-[#FBFBFA]">
        <div className="max-w-6xl mx-auto">
          <div className="text-center mb-12">
            <span className="text-[11px] font-semibold text-red-600 uppercase tracking-widest">The Silent Crisis</span>
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mt-3 mb-3">Meetings are broken. QMEET fixes them.</h2>
            <p className="text-lg text-gray-600 max-w-2xl mx-auto">$399 billion is lost every year because meeting decisions never turn into completed work.</p>
          </div>
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
            {[
              { value: "73%", label: "of action items forgotten within 48 hours", source: "Harvard Business Review" },
              { value: "31 hrs", label: "wasted per employee monthly in meetings", source: "Atlassian" },
              { value: "$25K", label: "annual meeting cost per employee", source: "Bain & Company" },
              { value: "84%", label: "of managers unhappy with follow-through", source: "McKinsey 2024" }
            ].map((s, i) => (
              <div key={i} className="p-6 bg-white rounded-xl border border-gray-200 hover-lift">
                <div className="text-3xl font-bold text-gray-900 mb-2">{s.value}</div>
                <div className="text-sm text-gray-600 mb-2">{s.label}</div>
                <div className="text-[10px] text-gray-400 font-semibold uppercase tracking-wider">ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â {s.source}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* 6 Agents */}
      <section id="features" className="py-20 px-6 bg-white border-y border-gray-100">
        <div className="max-w-6xl mx-auto">
          <div className="text-center mb-12">
            <span className="text-[11px] font-semibold text-[#2383E2] uppercase tracking-widest">Autonomous AI System</span>
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mt-3 mb-3">6 AI Agents working in concert</h2>
            <p className="text-lg text-gray-600 max-w-2xl mx-auto">Each agent has a specialized role. Together, they create autonomous accountability.</p>
          </div>
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4">
            {[
              { icon: FileText, name: "Transcription", desc: "Speech-to-text with speaker identification using Whisper Large V3", num: "01" },
              { icon: Target, name: "Extraction", desc: "Identifies commitments with owners, deadlines, priorities, dependencies", num: "02" },
              { icon: BarChart3, name: "Memory", desc: "Cross-references past meetings for patterns and repeat tasks", num: "03" },
              { icon: Zap, name: "Intelligence", desc: "Analyzes sentiment, participation, and calculates meeting ROI", num: "04" },
              { icon: Mail, name: "Communication", desc: "Auto-drafts and sends personalized follow-up emails", num: "05" },
              { icon: Shield, name: "Learning", desc: "Tracks completion rates and improves accountability over time", num: "06" }
            ].map((f, i) => (
              <div key={i} className="p-6 bg-white rounded-xl border border-gray-200 hover:border-[#2383E2] hover-lift group cursor-default">
                <div className="flex items-center justify-between mb-4">
                  <div className="w-10 h-10 rounded-lg bg-[#2383E2]/10 flex items-center justify-center group-hover:bg-[#2383E2] transition-colors">
                    <f.icon className="w-5 h-5 text-[#2383E2] group-hover:text-white transition-colors" />
                  </div>
                  <span className="text-xs font-mono text-gray-400 font-semibold">AGENT {f.num}</span>
                </div>
                <h3 className="text-base font-bold text-gray-900 mb-2">{f.name} Agent</h3>
                <p className="text-sm text-gray-600 leading-relaxed">{f.desc}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Demo */}
      <section id="demo" className="py-20 px-6 bg-white border-y border-gray-100">
        <div className="max-w-4xl mx-auto">
          <div className="text-center mb-10">
            <span className="text-[11px] font-semibold text-[#2383E2] uppercase tracking-widest">Try QMEET Now</span>
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mt-3 mb-3">Live demo Ãƒâ€šÃ‚Â· Free Ãƒâ€šÃ‚Â· No signup</h2>
            <p className="text-lg text-gray-600">Feed us a meeting. Watch 6 agents work in under 30 seconds.</p>
          </div>

          <div className="bg-white rounded-xl border border-gray-200 shadow-lg overflow-hidden">
            <div className="border-b border-gray-100 flex">
              {[
                { id: "demo", icon: Play, label: "Try sample meeting" },
                { id: "paste", icon: FileText, label: "Paste transcript" },
                { id: "upload", icon: Upload, label: "Upload audio" }
              ].map(m => (
                <button key={m.id} onClick={() => setMode(m.id)} className={"flex items-center gap-2 px-5 py-3.5 text-[13px] font-semibold border-b-2 transition-all -mb-px " + (mode === m.id ? "border-[#2383E2] text-[#2383E2]" : "border-transparent text-gray-500 hover:text-gray-900")}>
                  <m.icon className="w-3.5 h-3.5" />
                  {m.label}
                </button>
              ))}
            </div>

            {error && <div className="m-4 p-3 rounded-lg bg-red-50 border border-red-200 text-red-700 text-sm">{error}</div>}

            <div className="p-6">
              {mode === "demo" && (
                <div>
                  <div className="mb-4 flex items-center gap-2">
                    <Check className="w-4 h-4 text-green-600" />
                    <span className="text-sm font-semibold text-gray-900">Q3 Product Launch Meeting</span>
                    <span className="text-xs text-gray-500">Ãƒâ€šÃ‚Â· 5 participants Ãƒâ€šÃ‚Â· 15 min</span>
                  </div>
                  <div className="p-4 bg-gray-50 rounded-lg border border-gray-200 mb-4 max-h-48 overflow-y-auto">
                    <p className="text-sm text-gray-700 leading-relaxed font-mono">{DEMO_TRANSCRIPT.substring(0, 500)}...</p>
                  </div>
                  <button onClick={() => processTranscript(DEMO_TRANSCRIPT, "Q3 Product Launch Meeting")} className="group w-full flex items-center justify-center gap-2 py-3 rounded-lg bg-[#2383E2] text-white font-semibold text-sm hover:bg-[#1a6dc4] transition-all shadow-lg shadow-[#2383E2]/25">
                    <Sparkles className="w-4 h-4" />
                    Analyze with 6 AI Agents
                    <ArrowRight className="w-4 h-4 transition-transform group-hover:translate-x-0.5" />
                  </button>
                </div>
              )}
              {mode === "paste" && (
                <div>
                  <textarea value={transcript} onChange={(e) => setTranscript(e.target.value)} placeholder="Paste your meeting transcript here..." className="w-full h-48 p-4 bg-gray-50 border border-gray-200 rounded-lg text-gray-800 focus:outline-none focus:border-[#2383E2] focus:bg-white text-sm resize-none font-mono transition-all"></textarea>
                  <div className="mt-4 flex items-center justify-between">
                    <span className="text-xs text-gray-500">{transcript.split(" ").filter(Boolean).length} words</span>
                    <button onClick={() => processTranscript(transcript)} disabled={!transcript.trim()} className="flex items-center gap-2 px-5 py-2.5 rounded-lg bg-[#2383E2] text-white text-sm font-semibold disabled:opacity-30 hover:bg-[#1a6dc4] transition-colors">
                      Analyze Meeting
                      <ArrowRight className="w-4 h-4" />
                    </button>
                  </div>
                </div>
              )}
              {mode === "upload" && (
                <div onDragOver={(e) => { e.preventDefault(); setIsDragging(true) }} onDragLeave={() => setIsDragging(false)} onDrop={handleDrop} className={"relative border-2 border-dashed rounded-lg p-16 text-center transition-all " + (isDragging ? "border-[#2383E2] bg-blue-50" : "border-gray-300 bg-gray-50 hover:border-gray-400")}>
                  <input type="file" accept="audio/*,video/*" className="absolute inset-0 opacity-0 cursor-pointer w-full h-full" onChange={(e) => { const file = e.target.files[0]; if (file) processAudio(file) }} />
                  <Upload className="w-10 h-10 mx-auto text-[#2383E2] mb-3" />
                  <p className="text-gray-900 font-semibold mb-1">Drop your meeting recording here</p>
                  <p className="text-sm text-gray-500">MP3, WAV, M4A, MP4 ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â up to 25MB</p>
                </div>
              )}
            </div>
          </div>
        </div>
      </section>

      {/* Pricing */}
      <section id="pricing" className="py-20 px-6 bg-[#FBFBFA]">
        <div className="max-w-5xl mx-auto">
          <div className="text-center mb-12">
            <span className="text-[11px] font-semibold text-[#2383E2] uppercase tracking-widest">Pricing</span>
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mt-3 mb-3">Simple, transparent pricing</h2>
            <p className="text-lg text-gray-600">Start free. Upgrade when you need more.</p>
          </div>
          <div className="grid md:grid-cols-3 gap-4">
            {[
              { name: "Free", price: "Rs 0", desc: "Perfect for individuals", features: ["5 meetings per month", "Basic AI analysis", "Email follow-ups", "7-day history"], cta: "Get started", highlight: false, action: "signup" },
              { name: "Pro", price: "Rs 1,499", desc: "For growing teams", features: ["Unlimited meetings", "All 6 AI agents", "Auto follow-ups & escalations", "90-day history", "Slack integration", "Priority support"], cta: "Start free trial", highlight: true, action: "signup" },
              { name: "Enterprise", price: "Custom", desc: "For large organizations", features: ["Everything in Pro", "SSO & SAML", "On-premise deployment", "Custom integrations", "SLA guarantee", "Dedicated success manager"], cta: "Contact sales", highlight: false, action: "contact" }
            ].map((p, i) => (
              <div key={i} className={"p-6 rounded-xl border-2 hover-lift " + (p.highlight ? "border-[#2383E2] bg-white shadow-xl relative" : "border-gray-200 bg-white")}>
                {p.highlight && <div className="absolute -top-3 left-1/2 -translate-x-1/2 px-3 py-1 bg-[#2383E2] text-white text-[10px] font-bold uppercase tracking-wider rounded-full">Most Popular</div>}
                <div className="mb-4">
                  <h3 className="text-lg font-bold text-gray-900 mb-1">{p.name}</h3>
                  <p className="text-sm text-gray-500">{p.desc}</p>
                </div>
                <div className="mb-4">
                  <span className="text-4xl font-bold text-gray-900">{p.price}</span>
                  {p.price !== "Custom" && <span className="text-sm text-gray-500">/user/month</span>}
                </div>
                <button onClick={() => p.action === "contact" ? router.push("/contact") : router.push("/signup")} className={"w-full py-2.5 rounded-lg font-semibold text-sm mb-6 transition-all " + (p.highlight ? "bg-[#2383E2] text-white hover:bg-[#1a6dc4] shadow-md" : "border border-gray-300 text-gray-700 hover:bg-gray-50")}>{p.cta}</button>
                <div className="space-y-2">
                  {p.features.map((f, j) => (
                    <div key={j} className="flex items-start gap-2">
                      <Check className="w-4 h-4 text-green-600 mt-0.5 flex-shrink-0" />
                      <span className="text-sm text-gray-700">{f}</span>
                    </div>
                  ))}
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Final CTA */}
      <section className="py-20 px-6 bg-gradient-to-br from-[#2383E2] via-blue-600 to-blue-800 text-white text-center">
        <div className="max-w-3xl mx-auto">
          <h2 className="text-3xl md:text-5xl font-bold mb-4">Stop losing action items.</h2>
          <p className="text-lg opacity-90 mb-8">Join 1,247 teams using QMEET to make sure every meeting delivers results.</p>
          <button onClick={() => user ? router.push("/app") : router.push("/signup")} className="inline-flex items-center gap-2 px-6 py-3 bg-white text-[#2383E2] rounded-lg font-semibold hover:bg-gray-50 transition-colors shadow-xl hover:scale-105">
            {user ? "Go to Dashboard" : "Sign up free"}
            <ArrowRight className="w-4 h-4" />
          </button>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-12 px-6 border-t border-gray-100 bg-[#FBFBFA]">
        <div className="max-w-6xl mx-auto">
          <div className="grid md:grid-cols-4 gap-8 mb-8">
            <div>
              <div className="flex items-center gap-2 mb-3">
                <div className="w-7 h-7 rounded-lg bg-[#2383E2] flex items-center justify-center">
                  <Zap className="w-4 h-4 text-white" />
                </div>
                <span className="font-bold text-gray-900">QMEET</span>
              </div>
              <p className="text-xs text-gray-500">Autonomous meeting intelligence for teams that value their time.</p>
            </div>
            <div>
              <h4 className="text-xs font-bold text-gray-900 uppercase tracking-wider mb-3">Product</h4>
              <div className="space-y-2 text-sm">
                <a href="#features" className="block text-gray-600 hover:text-gray-900">Features</a>
                <a href="/app/integrations" className="block text-gray-600 hover:text-gray-900">Integrations</a>
                <a href="#pricing" className="block text-gray-600 hover:text-gray-900">Pricing</a>
                <a href="#demo" className="block text-gray-600 hover:text-gray-900">Live Demo</a>
              </div>
            </div>
            <div>
              <h4 className="text-xs font-bold text-gray-900 uppercase tracking-wider mb-3">Company</h4>
              <div className="space-y-2 text-sm">
                <a href="/contact" className="block text-gray-600 hover:text-gray-900">Contact</a>
                <a href="/contact" className="block text-gray-600 hover:text-gray-900">Support</a>
                <a href="/contact" className="block text-gray-600 hover:text-gray-900">Report a bug</a>
              </div>
            </div>
            <div>
              <h4 className="text-xs font-bold text-gray-900 uppercase tracking-wider mb-3">Legal</h4>
              <div className="space-y-2 text-sm">
                <a href="/privacy" className="block text-gray-600 hover:text-gray-900">Privacy Policy</a>
                <a href="/terms" className="block text-gray-600 hover:text-gray-900">Terms of Service</a>
                <a href="/privacy" className="block text-gray-600 hover:text-gray-900">Cookie Policy</a>
              </div>
            </div>
          </div>
          <div className="pt-6 border-t border-gray-200 flex flex-col md:flex-row items-center justify-between gap-3">
            <div className="text-xs text-gray-500">Ãƒâ€šÃ‚Â© 2026 QMEET Ãƒâ€šÃ‚Â· Built for Innova Hack Chapter 1 Ãƒâ€šÃ‚Â· Bengaluru, India</div>
            <div className="flex items-center gap-4">
              <a href="mailto:support@qmeet.ai" className="text-xs text-gray-500 hover:text-gray-900">support@qmeet.ai</a>
            </div>
          </div>
        </div>
      </footer>

      <CookieBanner />
    </div>
  )
}
